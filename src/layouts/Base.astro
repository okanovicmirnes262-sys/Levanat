---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { abs, businessSchema } from '../lib';

interface Props {
  title: string;
  description: string;
  /** Dodatni JSON-LD objekti (FAQPage, BreadcrumbList…) */
  schema?: object[];
  noindex?: boolean;
  ogImage?: string;
}

const { title, description, schema = [], noindex = false, ogImage = '/og.png' } = Astro.props;
const canonical = abs(Astro.url.pathname);
const ogUrl = abs(ogImage);
const jsonLd = [businessSchema(), ...schema];
---

<!doctype html>
<html lang="hr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    {noindex && <meta name="robots" content="noindex, follow" />}

    <link rel="preload" href="/fonts/fraunces.woff2" as="font" type="font/woff2" crossorigin />

    <link rel="icon" href="/favicon.ico" sizes="32x32" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta name="theme-color" content="#0b2a3c" />
    <link rel="sitemap" href="/sitemap-index.xml" />

    <meta property="og:type" content="website" />
    <meta property="og:locale" content="hr_HR" />
    <meta property="og:site_name" content="Levanat" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={ogUrl} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="Kamena glava s katedrale sv. Jakova — Levanat, web dizajn iz Šibenika" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogUrl} />

    {jsonLd.map((s) => <script type="application/ld+json" set:html={JSON.stringify(s)} />)}

    <script is:inline>
      // Kretanje uključujemo samo ako preglednik to želi. Ako se skripte ne učitaju,
      // sadržaj se nakon 5 s svejedno prikaže.
      (function () {
        var d = document.documentElement;
        if (!window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
          d.classList.add('motion');
          // Puni uvod (svjetlo preko glava, klesanje naslova) samo pri prvom otvaranju u sesiji.
          try {
            if (location.pathname === '/' && !sessionStorage.getItem('levanat.uvod')) {
              d.classList.add('uvod');
              sessionStorage.setItem('levanat.uvod', '1');
            }
          } catch (e) {
            if (location.pathname === '/') d.classList.add('uvod');
          }
          setTimeout(function () {
            if (!window.__levanatMotion) d.classList.remove('motion');
          }, 5000);
        }
      })();
    </script>
  </head>
  <body>
    <a class="skip-link" href="#sadrzaj">Preskoči na sadržaj</a>
    <Header />
    <main id="sadrzaj" tabindex="-1">
      <slot />
    </main>
    <Footer />
    <div class="kursor" aria-hidden="true" data-kursor></div>
    <script>
      import '../scripts/main';
    </script>
  </body>
</html>
